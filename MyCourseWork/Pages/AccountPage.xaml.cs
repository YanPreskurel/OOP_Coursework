using MyCourseWork.Entities;
using MyCourseWork.Services;

namespace MyCourseWork.Pages;

public partial class AccountPage : ContentPage
{
	public AccountPage()
	{
		InitializeComponent();
	}

    private void OnClickedAddAccount(object sender, EventArgs e)
    {
        //// add processing adding accounts
        //string name = "";
        //string tempCost = "";
        //string category = "";

        //if (CheckCorrect.MonetaryTurnover(tempCost))
        //{
        //    double cost = Convert.ToDouble(tempCost);
        //    DateTime dateTime = DateTime.Now;

        //    App.User.GetListAccounts().Add(new Account(name, cost, dateTime, category));
        //}
        //else
        //{
        //    // try again
        //}
    }
}